# Egress profile: observe
# Purpose: allow ANY destinations during window, but log new connections (kernel log prefix AEVUM_EGRESS_OBS)
# Use only inside a bounded egress window.
EGRESS_DEST_MODE=any
EGRESS_DNS_MODE=any
EGRESS_OBSERVE=1
