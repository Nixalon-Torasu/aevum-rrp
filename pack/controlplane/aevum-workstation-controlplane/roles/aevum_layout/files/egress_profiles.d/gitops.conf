# Egress profile: gitops
# Intended for: controlplane git pulls, apt updates, docker pulls for base images (if allowlisted).
EGRESS_DEST_MODE=allowlist
EGRESS_DNS_MODE=allowlist

# NOTE: domains/ips should be generated by:
#   /opt/aevum-tools/bin/aevum-pin-gitops-destinations
# which will write an updated profile file with populated domains.
