# Aevum bundle: observability
BUNDLE_NAME="Observability (node-exporter baseline)"
APT_PACKAGES=(
  prometheus-node-exporter
)
POST_COMMANDS=(
  "systemctl enable --now prometheus-node-exporter || true"
)
