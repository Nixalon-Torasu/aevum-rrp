# Aevum bundle: nvidia
# NOTE: This may prompt for MOK enrollment if Secure Boot is enabled.
BUNDLE_NAME="NVIDIA drivers (Ubuntu recommended)"
APT_PACKAGES=(
  ubuntu-drivers-common
)
POST_COMMANDS=(
  "ubuntu-drivers devices || true"
  "ubuntu-drivers install || true"
  "nvidia-smi || true"
)
