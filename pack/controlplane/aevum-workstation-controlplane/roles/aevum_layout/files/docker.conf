# Aevum Docker mode policy
# disabled: stop+mask docker & containerd (recommended for workstation lane if not needed)
# rootful : enable system docker service
# rootless: keep system docker disabled; rootless must be configured per-user (not auto here)
AEVUM_DOCKER_MODE=disabled
AEVUM_DOCKER_ROOTLESS_USER=
