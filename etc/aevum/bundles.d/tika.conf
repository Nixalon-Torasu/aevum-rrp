# Aevum bundle: tika
# Minimal foundation: Java runtime (Tika server jar pinning is done later).
BUNDLE_NAME="Tika foundation (Java runtime)"
APT_PACKAGES=(
  default-jre
)
POST_COMMANDS=(
  "java -version || true"
)
